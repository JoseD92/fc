union hola {
   int   holaid;
   int   algo;
};

struct Books {
   char  title[50];
   float mifloat;
   char  author[50];
   char  subject[100][2];
   int   book_id;
   struct Books *next;
   union hola unhola;
   bool btbt;
}; 

bool o;

int masuno(int x,bool b){
  float p;
  char c;
  int kk;
  return x+1;
  return;
  return;
  kk = x;
  return x+1;
}

void swap(ref int x,ref int y){
  int z;
  z = *x;
  *x = *y;
  *x = z;
}

int main(int m){
   int x,**y, z[9][4][5];
   int a[6][5],c[5];
   struct Books b;
   x = 1;2;
   x = 2 + 3 *19584 -84;
   x = masuno(x,True);
   masuno(x,True);
   swap(*y,*y);
   //write("hola");
   x = - 2;
   z[4][3][2] = 5;
   o = True || False && (5 > 6);
   o = o;
   **y=6;
   if (x > **y) { x = x + 1; }
   if (x == 0) {x = x + 1;} else {x = x - 1;}
   while (True && False) {
      break;
      continue;
      x=a[4][1];
      x=1;
   }
   for(x=1;;x<100;x=x+1;){
      break;
      continue;
      a[x][0] = x;
   }
   break;
   break;
}